#include <bits/stdc++.h>

using namespace std;

auto main() -> int {
    cin.tie(0)->sync_with_stdio(0);
    cout << fixed << setprecision(6);

    double n;
    cin >> n;

    cout << pow(n, 1 / n);
}
